# Happy Birthday Generator 🎂

A dynamic, modern birthday wish generator built with **Next.js**. Create personalized birthday cards with beautiful CSS art and animations just by sharing a link!

## ✨ Features

- **Dynamic Routing**: Generate a unique card for anyone by visiting `/AnyName`.
- **Glassmorphism UI**: A sleek, frosted-glass card design with a vibrant animated background.
- **CSS Art**: A fully animated birthday cake built entirely with CSS (no images!).
- **Confetti**: Interactive celebration effects using `canvas-confetti`.
- **Responsive**: Looks great on mobile and desktop.

## 🚀 Getting Started

1.  **Install dependencies**:
    ```bash
    npm install
    ```

2.  **Run the development server**:
    ```bash
    npm run dev
    ```

3.  **Open your browser**:
    Navigate to [http://localhost:3000](http://localhost:3000).

4.  **Celebrate**:
    Enter a name in the form, or share a direct link like `http://localhost:3000/Amna`.

## 🛠️ Built With

- **Next.js**: App Router & Server Components.
- **Tailwind CSS**: Utility-first styling.
- **TypeScript**: Type-safe code.
- **Canvas Confetti**: For the celebration effects.
